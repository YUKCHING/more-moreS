import Vue from 'vue'
import { Field, Button, Picker, Popup, DatetimePicker, Cell, CellGroup } from 'vant'

Vue.use(Field).use(Button).use(Picker).use(Popup).use(DatetimePicker).use(Cell).use(CellGroup)
