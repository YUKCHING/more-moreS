import Vue from 'vue'
import { Field, Button, Picker, Popup, DatetimePicker, Cell, CellGroup, Icon, Uploader, IndexBar, IndexAnchor, Tab, Tabs } from 'vant'

Vue.use(Field).use(Button).use(Picker).use(Popup).use(DatetimePicker).use(Cell).use(CellGroup).use(Icon).use(Uploader).use(IndexBar).use(IndexAnchor).use(Tab).use(Tabs)
